{% extends 'AppBookBundle::layout.html.twig'%}

{% block maincontent %}

    <div class="container">
        <div class="starter-template">
            <h1>Ajout d'un auteur</h1>
        </div>

        <div class="box-body">
            {{ include("AppBookBundle:Author:formAuthor.html.twig") }}
        </div>
    </div>

{% endblock maincontent %}
