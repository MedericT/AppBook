{% extends 'AppBookBundle::layout.html.twig'%}

{% block maincontent %}

    <div class="container">
        <div class="starter-template">
            <h1>Ajout d'un livre</h1>
        </div>

        <div class="box-body">
            {{ include("AppBookBundle:Book:formBook.html.twig") }}
        </div>
    </div>

{% endblock maincontent %}
