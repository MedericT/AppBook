{% extends 'AppBookBundle::layout.html.twig' %}

{% block maincontent %}

    <div class="container">
        <div class="starter-template">
            <h1>Livres</h1>
            {% if listBooks is empty %}
                <p class="lead">Il n'y a pour le moment aucun livre de disponnible</p>
            {% endif %}
                <p>
                    <a href="{{ path('appbook_book_add') }}" class="btn btn-custom-lighten">Ajouter un Livre</a>
                    <a href="#" class="btn btn-custom-lighten">Ajouter un Auteur</a>
                    <a href="#" class="btn btn-custom-lighten">Ajouter un Genre</a></p>
        </div>

    </div><!-- /.container -->

{% endblock %}
